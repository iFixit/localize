{
  "name": "@ifixit/localize",
  "version": "1.1.2",
  "homepage": "https://github.com/iFixit/localize",
  "main": "dist/translations.js",
  "types": "./localize.d.ts",
  "type": "module",
  "module": "dist/translations.js",
  "files": [
    "dist",
    "localize.d.ts"
  ],
  "scripts": {
    "test": "node --test test/",
    "build": "rimraf dist && NODE_ENV=production babel src --out-dir dist --copy-files --ignore __tests__,spec.js,test.js,__snapshots__"
  },
  "devDependencies": {
    "@babel/cli": "^8.0.4",
    "@babel/core": "^7.5.5",
    "@babel/preset-env": "^7.6.2",
    "rimraf": "^6.1.3"
  }
}
